correctpassword="admin123"
enteredpassword=""
attempts=0
maxattempts=3
while attempts<maxattempts:
    enteredpassword=input("enter the password:")
    if enteredpassword==correctpassword:
        print("Welcome to the profile")
        break
    else:
        attempts+=1
        if attempts<maxattempts:
            print(f"sorry wrong password! try again. you have {maxattempts-attempts} attempts left")
        else:
            print("sorry wrong password. no attempts left. kindly contact the branch")

